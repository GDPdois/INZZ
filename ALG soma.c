#include <stdio.h>
#include <conio.h>

Int main ()
     float p1, p2, total;

printf ("\nInsira o valor do produto");
scanf ("%f", &p1);
printf ("\nInsira o valor do outro produto");
scanf ("%f", &p2)

  total = p1 + p2;

printf ("\nTotal a pagar: %f", total);

return 0;
